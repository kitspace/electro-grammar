{
  "name": "parse-electronic-component",
  "version": "1.0.0",
  "main": "lib/index.js",
  "scripts": {
    "build": "nearleyc src/grammar.ne --out lib/grammar.js",
    "test": "yarn build && mocha"
  },
  "dependencies": {
    "nearley": "https://github.com/Hardmath123/nearley/",
    "ramda": "^0.23.0"
  },
  "devDependencies": {
    "mocha": "^3.3.0"
  }
}
