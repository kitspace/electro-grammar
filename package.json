{
  "name": "_electro-grammar",
  "devDependencies": {
    "better-assert": "^1.0.2",
    "js-yaml": "^3.9.1",
    "mocha": "^5.0.0",
    "nyc": "^11.4.1"
  },
  "scripts": {
    "test": "nyc -- mocha --check-leaks --recursive --timeout 6000",
    "coverage": "nyc report",
    "coverage:html": "nyc report --reporter=html"
  },
  "dependencies": {}
}
