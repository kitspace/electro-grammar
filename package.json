{
  "name": "parse-electronic-component",
  "version": "1.0.0",
  "main": "lib/index.js",
  "scripts": {
    "build:grammar": "nearleyc src/grammar.ne --out src/grammar.js",
    "build:lib": "babel --presets es2015 src -d lib",
    "build": "npm run build:grammar && npm run build:lib",
    "test": "npm run build && mocha"
  },
  "dependencies": {
    "nearley": "https://github.com/Hardmath123/nearley/",
    "ramda": "^0.23.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "mocha": "^3.3.0"
  }
}
